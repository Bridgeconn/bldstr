<div class="inner">
	<div class="footer-main">
		<div class="inner">
			<div class="footer-columns grid">
				<%if available_categories.size > 0 %>
					<% available_categories.each do |category| %>
						<div class="grid__item one-quarter">
							<h4><%= category.name.upcase %></h4>
							<ul class="pages-menu sf-menu sf-horizontal sf-js-enabled">
								<% category.children.each do |child_category| %>
									<li>
										<%= link_to child_category.name, category_path(child_category), class: "sf-with-ul" %>
									</li>
								<% end %>
							</ul>
						</div>
					<% end -%>
				<% end -%>
				<div class="grid__item one-quarter">
					<h4>CUSTOMER SUPPORT</h4>
					<ul class="pages-menu sf-menu sf-horizontal sf-js-enabled">
						<li><a href="http://bild.org" class="sf-with-ul" target="blank">BILD International</a></li>
						<li><a href="http://bridgeconn.com" class="sf-with-ul" target="blank">Bridge Connectivity Pvt. Ltd.</a></li>
						<li><a href="<%= new_contact_path %>" class="sf-with-ul">Contact Us</a></li>
					</ul>
				</div>
			</div>
		</div>
	</div>
	<div class="footer-links">
		<div class="inner">
			<p>
				©Copyright 2017, <a href="http://bridgeconn.com" target="blank">Bridge Connectivity Solutions</a> | All rights reserved |  <%= link_to "Privacy Policy", privacy_policy_path, target: "_blank" %> | <%= link_to "Shipping Policy", shipping_policy_path, target: "_blank" %> | <%= link_to "Terms & Condtions", terms_conditions_path, target: "_blank" %> | <%= link_to "About us", about_us_path, target: "_blank	" %>
			</p>
		</div>
	</div>
</div>
<table class="QuickSearch" id="QuickSearch" cellpadding="0" cellspacing="0" border="0" style="display:none;position: absolute; left: 265px; top: 57px; width: 390px;">
	<tbody id="product_search_list">
	</tbody>
</table>
<script type="text/javascript">
	$(document).mouseup(function(e)
	{
	    var container = $("#QuickSearch");
	
	    // if the target of the click isn't the container nor a descendant of the container
	    if (!container.is(e.target) && container.has(e.target).length === 0)
	    {
	        container.hide();
	    }
	});
</script>